[package]
name = "numerical_analysis"
version = "0.1.0"
edition = "2024"

[dependencies]
plotters = { version = "0.3", default-features = false, features = ["svg_backend", "line_series"] }
peroxide = { version = "0.40", features = [] }
anyhow = "1.0.100"
sanitize-filename = "0.6.0"
